@isTest
private class communityVerticalNavCtrlTest {
	
	@isTest static void test_method_one() {
        Account a = new Account(Name = 'Canapi');
        insert a;
        communityVerticalNavCtrl.getSettings();
    }

}